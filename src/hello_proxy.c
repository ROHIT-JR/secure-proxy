/* hello_proxy.c: Tiny program that proves the C toolchain, compiler flags, and Makefile wiring all work end to end. */
#include <stdio.h>

int main(void) {
    printf("hello_proxy: build system initialized successfully.\n");
    return 0;
}